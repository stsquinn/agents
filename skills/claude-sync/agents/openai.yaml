interface:
  display_name: "Claude Sync"
  short_description: "Sync ~/.claude config with the agents repo"
  default_prompt: "Use $claude-sync to publish my latest CLAUDE.md changes to the agents repo."
